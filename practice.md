# Практическое задание 1

1. Создайте модуль `my_module1.py` и добавьте в него функцию `weekday()` которая получает целое положительное число от 1 до 7 и возвращает название дня недели. Напишите эту функцию без использования оператора ветвления `if`.

2. Создайте пакет `my_package.py`, внутри него создайте модуль `my_module2.py` и добавьте в него 

3. Внутри пакета `my_package.py`, создайте вложенный пакет `sub_package` и внутри него модуль `my_module3.py`. Добавьте в `my_module3.py` функцию для 

4. Создайте стартовый скрипт `main.py` и добавьте в него импорты
